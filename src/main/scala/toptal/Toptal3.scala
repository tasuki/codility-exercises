package toptal

// get a list of integers: how many sublists can it be split into,
// so that when you sort each, the whole list is sorted

object Toptal3 {
  def solution(list: Seq[Int]): Int = {
    1
  }
}

